{
  "name": "@votechain/fixtures",
  "version": "1.0.0",
  "description": "Deterministic mock responses for Stellar RPC calls",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest",
    "test:coverage": "vitest --coverage"
  },
  "keywords": [
    "stellar",
    "soroban",
    "rpc",
    "mock",
    "fixture",
    "testing"
  ],
  "license": "MIT",
  "dependencies": {
    "@stellar/stellar-sdk": "^15.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  },
  "peerDependencies": {
    "@stellar/stellar-sdk": ">=12.0.0"
  }
}
